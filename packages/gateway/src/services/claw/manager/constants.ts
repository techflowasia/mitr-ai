/**
 * Claw Manager — Constants
 */

export const MAX_CONSECUTIVE_ERRORS = 5;
export const SESSION_PERSIST_INTERVAL_MS = 30_000;
export const MAX_CONCURRENT_CLAWS = 50;
export const CLEANUP_INTERVAL_MS = 24 * 60 * 60 * 1000; // once per day
export const MAX_INBOX_MESSAGES = 100;
export const MAX_INBOX_BYTES = 50_000;
